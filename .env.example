# Base URL for the Balanti backend API (see FRONTEND_API_GUIDE.md).
# Local Django dev server default shown below; point this at your deployed
# backend for staging/production builds.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
